# 🚨 紧急情况快速参考卡

## 代码崩了怎么办?

### 方法1: Git回滚 (推荐,最快)

```bash
cd "/Users/xingxiu/Desktop/Glaxe 项目抓取备份/GalxeMonitor"
./emergency_restore.sh
```

### 方法2: 从备份恢复

```bash
cd "/Users/xingxiu/Desktop/Glaxe 项目抓取备份/GalxeMonitor"
./restore_backup.sh
```

### 方法3: 从GitHub重新克隆

```bash
cd ~/Desktop
git clone git@github.com:yueyeye888-blip/Glaxe-.git GalxeMonitor_New
cd GalxeMonitor_New
git checkout 233949e
```

---

## 常用命令

### 备份

```bash
# 立即备份
./backup.sh
```

### 查看版本

```bash
# 查看当前版本
git log -1

# 查看所有版本
git log --oneline

# 当前稳定版本: 233949e
```

### 服务控制

```bash
# 停止服务
lsof -ti:5001 | xargs kill -9

# 启动服务
cd "/Users/xingxiu/Desktop/Glaxe 项目抓取备份/GalxeMonitor"
nohup python3 src/app.py > /dev/null 2>&1 &

# 查看日志
tail -f logs/app.log
```

---

## 保证100%恢复的条件

### ✅ 已经具备:
1. **GitHub仓库**: 所有代码都在GitHub
2. **Git提交记录**: 每次修改都有记录
3. **本地备份**: 使用 `backup.sh` 创建

### ⚠️ 需要单独备份:
1. **config.json**: 包含Bot Token等敏感信息
   - 位置: `config_files/config.json`
   - 备份方法: 复制到安全位置

2. **monitor_state.json**: 监控状态(可选)
   - 位置: `data/monitor_state.json`

---

## 最佳实践

### 每次修改代码前:

```bash
# 1. 创建备份
./backup.sh

# 2. 或创建测试分支
git checkout -b test-new-feature
```

### 修改后测试:

```bash
# 测试通知
python3 test_notification.py

# 如果正常,提交
git add -A
git commit -m "说明"
git push

# 如果有问题,回滚
git reset --hard HEAD
```

---

## GitHub 100%恢复流程

### 步骤1: 克隆代码
```bash
git clone git@github.com:yueyeye888-blip/Glaxe-.git
cd Glaxe-
git checkout 233949e  # 当前稳定版本
```

### 步骤2: 安装依赖
```bash
pip3 install -r requirements.txt
```

### 步骤3: 恢复配置
```bash
# 从备份复制
cp ~/备份位置/config.json config_files/config.json

# 或使用示例重建
cp config_files/config.multi_targets_example.json config_files/config.json
nano config_files/config.json  # 填写Bot Token等信息
```

### 步骤4: 启动服务
```bash
python3 src/app.py
```

---

## 关键信息

- **当前稳定版本**: `233949e`
- **GitHub仓库**: `yueyeye888-blip/Glaxe-`
- **备份目录**: `~/Desktop/GalxeMonitor_Backups`
- **服务端口**: `5001`
- **Web密码**: `admin`

---

## 联系方式

如果遇到无法解决的问题:
1. 查看日志: `tail -f logs/app.log`
2. 检查进程: `ps aux | grep python3`
3. 检查端口: `lsof -ti:5001`

---

**记住**: 只要GitHub上有代码,就永远不会丢失!
